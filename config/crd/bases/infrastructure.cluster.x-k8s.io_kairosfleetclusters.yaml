---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: kairosfleetclusters.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: KairosFleetCluster
    listKind: KairosFleetClusterList
    plural: kairosfleetclusters
    singular: kairosfleetcluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Whether the infrastructure cluster is provisioned
      jsonPath: .status.initialization.provisioned
      name: Provisioned
      type: boolean
    - description: Control plane endpoint host
      jsonPath: .spec.controlPlaneEndpoint.host
      name: Endpoint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          KairosFleetCluster is the Schema for the kairosfleetclusters API. It is the
          infrastructure side of a Cluster API Cluster backed by an AuroraBoot fleet.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KairosFleetClusterSpec defines the desired state of KairosFleetCluster.
            properties:
              auroraboot:
                description: |-
                  auroraboot describes the AuroraBoot fleet manager connection shared by all machines
                  belonging to this cluster.
                properties:
                  adminTokenSecretRef:
                    description: |-
                      adminTokenSecretRef references a Secret in the same namespace as the KairosFleetCluster
                      holding the AuroraBoot admin bearer token. The token is read from the Secret's "token"
                      data key and is never written to logs.
                    properties:
                      name:
                        description: name is the name of the referenced Secret.
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  url:
                    description: url is the base URL of the AuroraBoot fleet API,
                      e.g. "https://auroraboot.example:8080".
                    minLength: 1
                    type: string
                required:
                - adminTokenSecretRef
                - url
                type: object
              controlPlaneEndpoint:
                description: |-
                  controlPlaneEndpoint represents the endpoint used to communicate with the control
                  plane. For a fleet cluster the endpoint is operator-supplied (for example a kube-vip
                  VIP or a managed load balancer); this provider does not allocate it.
                  status.initialization.provisioned gates on it being set.
                minProperties: 1
                properties:
                  host:
                    description: host is the hostname on which the API server is serving.
                    maxLength: 512
                    minLength: 1
                    type: string
                  port:
                    description: port is the port on which the API server is serving.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                type: object
            required:
            - auroraboot
            type: object
          status:
            description: KairosFleetClusterStatus defines the observed state of KairosFleetCluster.
            properties:
              conditions:
                description: conditions represents the observations of a KairosFleetCluster's
                  current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failureMessage:
                description: |-
                  failureMessage is a terminal, human-readable description of a failure. It is set only
                  when the cluster has entered an unrecoverable state.
                type: string
              failureReason:
                description: |-
                  failureReason is a terminal, programmatic failure indication. It is set only when the
                  cluster has entered an unrecoverable state. (Deprecated in the CAPI v1beta2 contract
                  but still surfaced.)
                type: string
              initialization:
                description: initialization provides observations of the KairosFleetCluster
                  initialization process.
                properties:
                  provisioned:
                    description: |-
                      provisioned is true when the infrastructure cluster is fully provisioned. This is the
                      Cluster API v1beta2 InfraCluster readiness signal read by the core Cluster controller.
                    type: boolean
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
